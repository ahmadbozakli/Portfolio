/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'placehold.co',
      "ik.imagekit.io",
      "em-content.zobj.net",
    ]
  }
};

module.exports = nextConfig;